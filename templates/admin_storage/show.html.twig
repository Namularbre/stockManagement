{% extends 'base.html.twig' %}

{% block title %}Storage{% endblock %}

{% block body %}
    <h1>Storage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ storage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ storage.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="btn-group mb-3">
        <a class="btn btn-secondary" href="{{ path('app_admin_storage_index') }}">back to list</a>
        <a class="btn btn-primary" href="{{ path('app_admin_storage_edit', {'id': storage.id}) }}">edit</a>
    </div>

    {{ include('admin_storage/_delete_form.html.twig') }}
{% endblock %}
